{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"sqlmodel-cache","text":"<p>Transparent Redis caching for SQLModel \u2014 zero call-site changes required.</p>"},{"location":"#overview","title":"Overview","text":"<p><code>sqlmodel-cache</code> adds automatic Redis caching to <code>session.get()</code> calls on SQLModel models. Declare caching intent on a model class; the library handles the rest.</p> <p>Status: Alpha \u2014 under active development. API subject to change before 1.0.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install sqlmodel-cache\n</code></pre> <p>For async support:</p> <pre><code>pip install \"sqlmodel-cache[async]\"\n</code></pre>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>from sqlmodel_cache import CacheConfig, SQLModelCache\nfrom sqlmodel_cache.transport import RedisSyncTransport\nimport redis\n\n# Configure once at startup\nSQLModelCache.configure(\n    transport=RedisSyncTransport(redis.Redis.from_url(\"redis://localhost:6379\")),\n    default_ttl=300,\n)\n\n# Declare caching on your model\nclass Hero(SQLModel, table=True):\n    __cache_config__ = CacheConfig(ttl=300)\n    id: int | None = Field(default=None, primary_key=True)\n    name: str\n\n# session.get() automatically reads from / writes to Redis\nwith Session(engine) as session:\n    hero = session.get(Hero, 1)  # cache miss \u2192 DB + populate Redis\n    hero = session.get(Hero, 1)  # cache hit \u2192 Redis only, no DB query\n</code></pre> <p>See the Quickstart guide for full setup instructions.</p>"},{"location":"advanced/","title":"Advanced Usage","text":""},{"location":"advanced/#per-call-cache-bypass","title":"Per-call Cache Bypass","text":"<p>Disable caching for a single <code>session.get()</code> without touching model configuration:</p> <pre><code>with Session(engine) as session:\n    # Always queries the database \u2014 never reads or writes the cache\n    hero = session.get(Hero, 1, execution_options={\"cache\": False})\n</code></pre> <p>Useful for admin reads, consistency-critical paths, or debugging.</p>"},{"location":"advanced/#per-call-ttl-override","title":"Per-call TTL Override","text":"<p>Override the model's TTL for a single call:</p> <pre><code>with Session(engine) as session:\n    # Cache this result for 60 seconds regardless of CacheConfig.ttl\n    hero = session.get(Hero, 1, execution_options={\"cache_ttl\": 60})\n</code></pre> <p>Precedence order (highest to lowest):</p> <ol> <li><code>execution_options(cache_ttl=N)</code> \u2014 per-call</li> <li><code>CacheConfig(ttl=N)</code> \u2014 per-model</li> <li><code>SQLModelCache.configure(default_ttl=N)</code> \u2014 global</li> </ol>"},{"location":"advanced/#async-sessions","title":"Async Sessions","text":"<p>Use <code>RedisAsyncTransport</code> with <code>AsyncSession</code> in async frameworks (FastAPI, Litestar\u2026):</p> <pre><code>import redis.asyncio\nfrom sqlmodel_cache import SQLModelCache\nfrom sqlmodel_cache.transport import RedisAsyncTransport\n\n# Configure at startup (e.g. FastAPI lifespan)\nr = redis.asyncio.Redis.from_url(\"redis://localhost:6379\")\nSQLModelCache.configure(transport=RedisAsyncTransport(r), default_ttl=300)\n\n# Async session.get() is cached transparently\nfrom sqlmodel.ext.asyncio.session import AsyncSession\n\nasync with AsyncSession(async_engine) as session:\n    hero = await session.get(Hero, 1)  # cache miss \u2192 DB + populate cache\n    hero = await session.get(Hero, 1)  # cache hit \u2192 Redis only\n</code></pre>"},{"location":"advanced/#sync-async-session-coexistence","title":"Sync + Async Session Coexistence","text":"<p>You can configure both sync and async sessions with a single transport. They share the same Redis keyspace so a sync commit automatically invalidates keys visible to async sessions:</p> <pre><code>import redis.asyncio\nfrom sqlmodel_cache import SQLModelCache\nfrom sqlmodel_cache.transport import RedisAsyncTransport\n\nr = redis.asyncio.Redis.from_url(\"redis://localhost:6379\")\nSQLModelCache.configure(transport=RedisAsyncTransport(r), default_ttl=300)\n\n# Sync write \u2192 cache invalidated \u2192 async session sees fresh value\nwith Session(engine) as session:\n    hero = session.get(Hero, 1)\n    hero.name = \"New Name\"\n    session.commit()  # invalidates the cache key\n\nasync with AsyncSession(async_engine) as session:\n    hero = await session.get(Hero, 1)  # cache miss \u2192 fresh DB read\n</code></pre> <p>Single transport instance</p> <p><code>SQLModelCache.configure()</code> accepts one transport. If your app mixes sync and async sessions, use <code>RedisAsyncTransport</code> \u2014 the library's greenlet bridge makes it work for both session types.</p>"},{"location":"advanced/#test-isolation","title":"Test Isolation","text":"<p>Prevent cache state bleeding between tests with an <code>autouse</code> fixture:</p> <pre><code># tests/conftest.py\nimport pytest\nfrom sqlmodel_cache import SQLModelCache\n\n@pytest.fixture(autouse=True)\ndef reset_cache():\n    yield\n    SQLModelCache.reset()\n</code></pre> <p>For full integration tests against a real Redis instance:</p> <pre><code>import os\nimport pytest\nimport redis\nfrom sqlmodel_cache import SQLModelCache\nfrom sqlmodel_cache.transport import RedisSyncTransport\n\n@pytest.fixture(scope=\"session\")\ndef redis_url():\n    if url := os.environ.get(\"REDIS_URL\"):\n        yield url\n        return\n    from testcontainers.redis import RedisContainer\n    with RedisContainer() as container:\n        host = container.get_container_host_ip()\n        port = container.get_exposed_port(6379)\n        yield f\"redis://{host}:{port}\"\n\n@pytest.fixture\ndef configured_cache(redis_url):\n    SQLModelCache.configure(\n        transport=RedisSyncTransport(redis.Redis.from_url(redis_url)),\n        default_ttl=60,\n    )\n    yield\n    SQLModelCache.reset()\n</code></pre>"},{"location":"advanced/#custom-transport","title":"Custom Transport","text":"<p>Implement the <code>CacheTransport</code> protocol to use any backend (Memcached, DynamoDB, in-memory\u2026):</p> <pre><code>from sqlmodel_cache.transport import CacheTransport\n\nclass InMemoryTransport:\n    \"\"\"Simple dict-backed transport for testing.\"\"\"\n\n    def __init__(self) -&gt; None:\n        self._store: dict[str, bytes] = {}\n\n    def get(self, key: str) -&gt; bytes | None:\n        return self._store.get(key)\n\n    def set(self, key: str, value: bytes, ttl: int) -&gt; None:\n        self._store[key] = value  # TTL not enforced in this example\n\n    def delete(self, *keys: str) -&gt; None:\n        for key in keys:\n            self._store.pop(key, None)\n\n# Use it:\nSQLModelCache.configure(transport=InMemoryTransport(), default_ttl=300)\n</code></pre> <p>No inheritance required \u2014 structural subtyping (Protocol) is used.</p>"},{"location":"advanced/#disabling-the-cache-globally","title":"Disabling the Cache Globally","text":"<p>Set <code>enabled=False</code> to make the library a no-op without removing configuration:</p> <pre><code>SQLModelCache.configure(\n    transport=RedisSyncTransport(redis.Redis.from_url(\"redis://localhost:6379\")),\n    default_ttl=300,\n    enabled=False,  # all session.get() calls pass through to DB\n)\n</code></pre> <p>Useful for feature flags or gradual rollouts.</p>"},{"location":"advanced/#cache-key-format","title":"Cache Key Format","text":"<p>Keys follow this deterministic format:</p> <pre><code>sqlmodelcache:{ModelClassName}:{field1}={value1}:{field2}={value2}\n</code></pre> <ul> <li>Fields are sorted alphabetically for composite PKs (reproducible regardless of dict insertion order)</li> <li><code>ModelClassName</code> preserves the Python class name casing</li> <li>Override the prefix via <code>SQLModelCache.configure(key_prefix=\"myapp\")</code></li> </ul> <p>Example for <code>Hero</code> with <code>id=42</code>: <pre><code>sqlmodelcache:Hero:id=42\n</code></pre></p> <p>Example for a composite PK model <code>OrderItem(order_id=1, product_id=5)</code>: <pre><code>sqlmodelcache:OrderItem:order_id=1:product_id=5\n</code></pre></p>"},{"location":"advanced/#test-isolation_1","title":"Test Isolation","text":"<pre><code>@pytest.fixture(autouse=True)\ndef reset_cache():\n    yield\n    SQLModelCache.reset()\n</code></pre>"},{"location":"api-reference/","title":"API Reference","text":""},{"location":"api-reference/#core","title":"Core","text":""},{"location":"api-reference/#cacheconfig","title":"CacheConfig","text":""},{"location":"api-reference/#sqlmodel_cache.CacheConfig","title":"<code>sqlmodel_cache.CacheConfig</code>  <code>dataclass</code>","text":"<p>Per-model cache configuration.</p> <p>Assign as a class variable on any SQLModel subclass::</p> <pre><code>class Hero(SQLModel, table=True):\n    __cache_config__ = CacheConfig(ttl=300)\n    id: int | None = Field(default=None, primary_key=True)\n    name: str\n</code></pre> <p>Attributes:</p> Name Type Description <code>enabled</code> <code>bool</code> <p>When <code>False</code>, caching is disabled for this model even if the library is configured.  Defaults to <code>True</code>.</p> <code>ttl</code> <code>int | None</code> <p>Time-to-live in seconds for cached entries.  <code>None</code> means fall back to the global <code>default_ttl</code> set in <code>SQLModelCache.configure()</code>.</p>"},{"location":"api-reference/#sqlmodelcache","title":"SQLModelCache","text":""},{"location":"api-reference/#sqlmodel_cache.SQLModelCache","title":"<code>sqlmodel_cache.SQLModelCache</code>","text":"<p>Application-level interface for configuring sqlmodel-cache.</p> <p>Call <code>configure()</code> once at startup before any SQLModel session operations on cache-enabled models.</p>"},{"location":"api-reference/#sqlmodel_cache.SQLModelCache.configure","title":"<code>configure(transport, *, default_ttl=300, key_prefix='sqlmodelcache', enabled=True)</code>  <code>classmethod</code>","text":"<p>Configure the library with a transport and global defaults.</p> <p>Parameters:</p> Name Type Description Default <code>transport</code> <code>CacheTransport | AsyncCacheTransport</code> <p>A <code>CacheTransport</code> implementation (e.g. <code>RedisSyncTransport</code>).  <code>FakeTransport</code> from <code>tests/unit/fakes.py</code> is suitable for tests.</p> required <code>default_ttl</code> <code>int</code> <p>Default TTL in seconds for cached entries when the model's <code>CacheConfig.ttl</code> is <code>None</code>.  Defaults to 300.</p> <code>300</code> <code>key_prefix</code> <code>str</code> <p>Redis key prefix.  Defaults to <code>\"sqlmodelcache\"</code>. Override per-deployment to avoid collisions across apps.</p> <code>'sqlmodelcache'</code> <code>enabled</code> <code>bool</code> <p>When <code>False</code>, the library is fully disabled \u2014 no cache reads, writes, or invalidations occur.  All <code>session.get()</code> calls fall through to the database.  Defaults to <code>True</code>.</p> <code>True</code>"},{"location":"api-reference/#sqlmodel_cache.SQLModelCache.get_config","title":"<code>get_config()</code>  <code>classmethod</code>","text":"<p>Return active configuration; raises ConfigurationError if unconfigured.</p>"},{"location":"api-reference/#sqlmodel_cache.SQLModelCache.reset","title":"<code>reset()</code>  <code>classmethod</code>","text":"<p>Clear configuration and remove event listeners.</p> <p>Intended exclusively for test isolation \u2014 call in pytest fixture teardown to prevent state leaking between tests::</p> <pre><code>@pytest.fixture(autouse=True)\ndef reset_cache():\n    yield\n    SQLModelCache.reset()\n</code></pre>"},{"location":"api-reference/#errors","title":"Errors","text":""},{"location":"api-reference/#cacheerror","title":"CacheError","text":""},{"location":"api-reference/#sqlmodel_cache.CacheError","title":"<code>sqlmodel_cache.CacheError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for all sqlmodel-cache errors.</p> <p>Catch this to handle any error raised by the library::</p> <pre><code>try:\n    session.get(Hero, 1)\nexcept CacheError:\n    ...  # library-level error (not Redis errors \u2014 those are suppressed)\n</code></pre>"},{"location":"api-reference/#configurationerror","title":"ConfigurationError","text":""},{"location":"api-reference/#sqlmodel_cache.ConfigurationError","title":"<code>sqlmodel_cache.ConfigurationError</code>","text":"<p>               Bases: <code>CacheError</code></p> <p>Raised when a cache operation is attempted before configuration.</p> <p>This is raised by <code>_state.get_config()</code> when <code>SQLModelCache.configure()</code> has not yet been called at application startup::</p> <pre><code># triggers ConfigurationError:\nsession.get(Hero, 1)  # before SQLModelCache.configure(...)\n</code></pre> <p>Resolution: call <code>SQLModelCache.configure(transport=..., default_ttl=...)</code> once at application startup before any SQLModel session operations.</p>"},{"location":"api-reference/#transports","title":"Transports","text":""},{"location":"api-reference/#redissynctransport","title":"RedisSyncTransport","text":""},{"location":"api-reference/#sqlmodel_cache.transport.RedisSyncTransport","title":"<code>sqlmodel_cache.transport.RedisSyncTransport</code>","text":"<p>Synchronous Redis transport wrapping <code>redis.Redis</code>.</p> <p>Satisfies :class:<code>~sqlmodel_cache.transport._protocols.CacheTransport</code> structurally \u2014 no inheritance required.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>Redis</code> <p>A configured <code>redis.Redis</code> instance.</p> required"},{"location":"api-reference/#sqlmodel_cache.transport.RedisSyncTransport.delete","title":"<code>delete(*keys)</code>","text":"<p>Remove one or more keys from the cache.</p>"},{"location":"api-reference/#sqlmodel_cache.transport.RedisSyncTransport.get","title":"<code>get(key)</code>","text":"<p>Return cached bytes or <code>None</code> if the key does not exist.</p>"},{"location":"api-reference/#sqlmodel_cache.transport.RedisSyncTransport.set","title":"<code>set(key, value, ttl)</code>","text":"<p>Store <code>value</code> under <code>key</code> with an expiry of <code>ttl</code> seconds.</p>"},{"location":"api-reference/#redisasynctransport","title":"RedisAsyncTransport","text":""},{"location":"api-reference/#sqlmodel_cache.transport.RedisAsyncTransport","title":"<code>sqlmodel_cache.transport.RedisAsyncTransport</code>","text":"<p>Asynchronous Redis transport wrapping <code>redis.asyncio.Redis</code>.</p> <p>Satisfies :class:<code>~sqlmodel_cache.transport._protocols.AsyncCacheTransport</code> structurally \u2014 no inheritance required.</p> <p>Parameters:</p> Name Type Description Default <code>client</code> <code>Redis</code> <p>A configured <code>redis.asyncio.Redis</code> instance.</p> required Note <p><code>redis.asyncio</code> is NOT imported at module level \u2014 this ensures no event loop is required at import time (NFR-COMPAT-5).</p>"},{"location":"api-reference/#sqlmodel_cache.transport.RedisAsyncTransport.delete","title":"<code>delete(*keys)</code>  <code>async</code>","text":"<p>Remove one or more keys from the cache.</p>"},{"location":"api-reference/#sqlmodel_cache.transport.RedisAsyncTransport.get","title":"<code>get(key)</code>  <code>async</code>","text":"<p>Return cached bytes or <code>None</code> if the key does not exist.</p>"},{"location":"api-reference/#sqlmodel_cache.transport.RedisAsyncTransport.set","title":"<code>set(key, value, ttl)</code>  <code>async</code>","text":"<p>Store <code>value</code> under <code>key</code> with an expiry of <code>ttl</code> seconds.</p>"},{"location":"api-reference/#per-request-control-execution_options","title":"Per-Request Control (<code>execution_options</code>)","text":"<p>Pass these keys to <code>execution_options</code> on any <code>session.get()</code> call to control caching on a per-call basis without changing the model's <code>CacheConfig</code>.</p> Key Type Description <code>cache</code> <code>bool</code> Set to <code>False</code> to bypass the cache for this call entirely <code>cache_ttl</code> <code>int</code> Override TTL (seconds) for this specific call <p>Example \u2014 Bypass cache for a single call:</p> <pre><code>with Session(engine) as session:\n    # Always queries the database, never reads or writes the cache\n    hero = session.get(Hero, 1, execution_options={\"cache\": False})\n</code></pre> <p>Example \u2014 Custom TTL for a single call:</p> <pre><code>with Session(engine) as session:\n    # Cache this result for 60 seconds, ignoring the model's __cache_config__.ttl\n    hero = session.get(Hero, 1, execution_options={\"cache_ttl\": 60})\n</code></pre>"},{"location":"api-reference/#cacheconfig-field-reference","title":"<code>CacheConfig</code> Field Reference","text":"Field Type Default Description <code>enabled</code> <code>bool</code> <code>True</code> When <code>False</code>, all <code>session.get()</code> calls for this model skip the cache <code>ttl</code> <code>int \\| None</code> <code>None</code> TTL in seconds; <code>None</code> falls back to <code>SQLModelCache.configure(default_ttl=...)</code>"},{"location":"api-reference/#sqlmodelcacheconfigure-parameter-reference","title":"<code>SQLModelCache.configure()</code> Parameter Reference","text":"Parameter Type Default Description <code>transport</code> <code>CacheTransport \\| AsyncCacheTransport</code> required Redis transport instance <code>default_ttl</code> <code>int</code> <code>300</code> Default TTL (seconds) for models without an explicit <code>CacheConfig.ttl</code> <code>key_prefix</code> <code>str</code> <code>\"sqlmodelcache\"</code> Redis key prefix; override per-deployment to avoid key collisions <code>enabled</code> <code>bool</code> <code>True</code> Global kill switch; <code>False</code> disables all caching, reads, and invalidations <p>Raised when cache operations are attempted before <code>SQLModelCache.configure()</code> is called.</p>"},{"location":"api-reference/#sqlmodel_cachetransport","title":"<code>sqlmodel_cache.transport</code>","text":""},{"location":"api-reference/#redissynctransport_1","title":"<code>RedisSyncTransport</code>","text":"<p>Synchronous Redis transport wrapping <code>redis.Redis</code>.</p>"},{"location":"api-reference/#redisasynctransport_1","title":"<code>RedisAsyncTransport</code>","text":"<p>Asynchronous Redis transport wrapping <code>redis.asyncio.Redis</code>.</p>"},{"location":"migration/","title":"Migration Guide","text":"<p>Adding <code>sqlmodel-cache</code> to an existing SQLModel application is designed to be non-breaking and incremental. Each change below is independent \u2014 you can apply them one at a time and verify your app still works before moving on.</p>"},{"location":"migration/#prerequisites","title":"Prerequisites","text":"<ul> <li>A working SQLModel application that uses <code>session.get()</code> for primary-key lookups</li> <li>A running Redis instance (local or remote)</li> <li><code>sqlmodel-cache</code> installed: <code>pip install sqlmodel-cache</code></li> </ul>"},{"location":"migration/#step-1-install-the-library","title":"Step 1: Install the Library","text":"<pre><code>pip install sqlmodel-cache\n# For async FastAPI apps:\npip install \"sqlmodel-cache[async]\"\n</code></pre>"},{"location":"migration/#step-2-configure-at-startup","title":"Step 2: Configure at Startup","text":"<p>You need to call <code>SQLModelCache.configure()</code> once before any request is handled. The location of this call depends on your framework.</p>"},{"location":"migration/#plain-script","title":"Plain script","text":"<p>Before:</p> <pre><code># main.py \u2014 plain SQLModel, no cache\nfrom sqlmodel import create_engine\n\nengine = create_engine(\"postgresql+psycopg://user:pass@localhost/mydb\")\n</code></pre> <p>After:</p> <pre><code># main.py \u2014 with sqlmodel-cache\nimport redis\nfrom sqlmodel import create_engine\nfrom sqlmodel_cache import SQLModelCache\nfrom sqlmodel_cache.transport import RedisSyncTransport\n\nengine = create_engine(\"postgresql+psycopg://user:pass@localhost/mydb\")\n\nSQLModelCache.configure(\n    transport=RedisSyncTransport(redis.Redis.from_url(\"redis://localhost:6379\")),\n    default_ttl=300,  # 5 minutes\n)\n</code></pre>"},{"location":"migration/#fastapi-sync-transport","title":"FastAPI (sync transport)","text":"<pre><code>import redis\nfrom contextlib import asynccontextmanager\nfrom fastapi import FastAPI\nfrom sqlmodel_cache import SQLModelCache\nfrom sqlmodel_cache.transport import RedisSyncTransport\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    r = redis.Redis.from_url(\"redis://localhost:6379\")\n    SQLModelCache.configure(\n        transport=RedisSyncTransport(r),\n        default_ttl=300,\n    )\n    yield\n    r.close()\n\napp = FastAPI(lifespan=lifespan)\n</code></pre>"},{"location":"migration/#fastapi-async-transport","title":"FastAPI (async transport)","text":"<pre><code>import redis.asyncio\nfrom contextlib import asynccontextmanager\nfrom fastapi import FastAPI\nfrom sqlmodel_cache import SQLModelCache\nfrom sqlmodel_cache.transport import RedisAsyncTransport\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    r = redis.asyncio.Redis.from_url(\"redis://localhost:6379\")\n    SQLModelCache.configure(\n        transport=RedisAsyncTransport(r),\n        default_ttl=300,\n    )\n    yield\n    await r.aclose()\n\napp = FastAPI(lifespan=lifespan)\n</code></pre>"},{"location":"migration/#step-3-opt-models-in-to-caching","title":"Step 3: Opt Models In to Caching","text":"<p>Add <code>__cache_config__</code> to any model whose <code>session.get()</code> lookups should be cached. Models without <code>__cache_config__</code> are never cached.</p> <p>Before:</p> <pre><code>from sqlmodel import Field, SQLModel\n\nclass Hero(SQLModel, table=True):\n    id: int | None = Field(default=None, primary_key=True)\n    name: str = \"\"\n</code></pre> <p>After:</p> <pre><code>from sqlmodel import Field, SQLModel\nfrom sqlmodel_cache import CacheConfig\n\nclass Hero(SQLModel, table=True):\n    __cache_config__ = CacheConfig(ttl=600)  # 10-minute TTL\n    id: int | None = Field(default=None, primary_key=True)\n    name: str = \"\"\n</code></pre> <p>That's it. All <code>session.get(Hero, pk)</code> calls are now transparently cached. No other code changes are required.</p>"},{"location":"migration/#testing-with-sqlmodel-cache","title":"Testing with sqlmodel-cache","text":"<p>Add an <code>autouse</code> fixture to your <code>conftest.py</code> so each test starts with a clean configuration and state cannot bleed between tests:</p> <pre><code># tests/conftest.py\nimport pytest\nfrom sqlmodel_cache import SQLModelCache\n\n@pytest.fixture(autouse=True)\ndef reset_cache():\n    \"\"\"Ensure every test starts with a clean cache configuration.\"\"\"\n    yield\n    SQLModelCache.reset()\n</code></pre> <p>If you use testcontainers for a live Redis instance in integration tests and also run in CI with a managed Redis service, allow <code>REDIS_URL</code> to override:</p> <pre><code>import os\nimport pytest\nimport redis\nfrom sqlmodel_cache import SQLModelCache\nfrom sqlmodel_cache.transport import RedisSyncTransport\n\n@pytest.fixture(scope=\"session\")\ndef redis_url():\n    # In CI, REDIS_URL is set by the GitHub Actions services block.\n    # Locally, fall back to testcontainers.\n    if url := os.environ.get(\"REDIS_URL\"):\n        yield url\n        return\n    from testcontainers.redis import RedisContainer\n    with RedisContainer() as container:\n        host = container.get_container_host_ip()\n        port = container.get_exposed_port(6379)\n        yield f\"redis://{host}:{port}\"\n\n@pytest.fixture\ndef configured_cache(redis_url):\n    SQLModelCache.configure(\n        transport=RedisSyncTransport(redis.Redis.from_url(redis_url)),\n        default_ttl=60,\n    )\n    yield\n    SQLModelCache.reset()\n</code></pre>"},{"location":"migration/#selectively-bypassing-the-cache","title":"Selectively Bypassing the Cache","text":"<pre><code>with Session(engine) as session:\n    # Always goes to the database; never reads or writes the cache\n    hero = session.get(Hero, 1, execution_options={\"cache\": False})\n\n    # Caches this result for 60 seconds instead of the model's configured TTL\n    hero = session.get(Hero, 1, execution_options={\"cache_ttl\": 60})\n</code></pre>"},{"location":"migration/#whats-cached-what-isnt","title":"What's Cached, What Isn't","text":"Operation Cached? <code>session.get(Hero, 1)</code> \u2705 Yes <code>session.exec(select(Hero))</code> \u274c No (pass-through to DB) <code>session.exec(select(Hero).where(...))</code> \u274c No (pass-through to DB) INSERT / UPDATE / DELETE via <code>session.commit()</code> \u274c Never (auto-invalidates affected cache keys) <p>Only <code>session.get()</code> is cached</p> <p><code>sqlmodel-cache</code> intercepts only primary-key lookups via <code>session.get()</code>. All other queries pass through to the database unchanged.</p>"},{"location":"migration/#troubleshooting","title":"Troubleshooting","text":"<p><code>ConfigurationError: SQLModelCache is not configured</code> \u2192 You forgot to call <code>SQLModelCache.configure()</code>. Add it to your application's startup code or FastAPI lifespan before handling any requests.</p> <p><code>ConfigurationError: SQLModelCache is already configured</code> \u2192 <code>configure()</code> was called twice without a <code>reset()</code> in between. In tests, add the <code>autouse</code> reset fixture shown above. In production code, guard with a flag or call <code>reset()</code> before reconfiguring.</p> <p>Cached values not updating after a write \u2192 Confirm the model has <code>__cache_config__</code> set. Without it, the after-commit invalidation hook ignores rows of that model entirely and no cache keys are ever written or evicted. Add <code>__cache_config__ = CacheConfig()</code> to enable full lifecycle management.</p>"},{"location":"quickstart/","title":"Quickstart","text":"<p>Get a working cached <code>session.get()</code> in under 5 minutes.</p>"},{"location":"quickstart/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.11 or later</li> <li>A running Redis instance (<code>redis://localhost:6379</code>)</li> </ul>"},{"location":"quickstart/#installation","title":"Installation","text":"<pre><code>pip install sqlmodel-cache\n</code></pre> <p>For async support:</p> <pre><code>pip install \"sqlmodel-cache[async]\"\n</code></pre>"},{"location":"quickstart/#sync-quickstart","title":"Sync Quickstart","text":""},{"location":"quickstart/#1-configure-at-startup","title":"1. Configure at startup","text":"<p>Call <code>SQLModelCache.configure()</code> once before handling any requests.</p> <pre><code>import redis\nfrom sqlmodel_cache import SQLModelCache\nfrom sqlmodel_cache.transport import RedisSyncTransport\n\nSQLModelCache.configure(\n    transport=RedisSyncTransport(redis.Redis.from_url(\"redis://localhost:6379\")),\n    default_ttl=300,  # 5-minute default TTL\n)\n</code></pre>"},{"location":"quickstart/#2-declare-a-cached-model","title":"2. Declare a cached model","text":"<p>Add <code>__cache_config__</code> to any <code>SQLModel</code> table class:</p> <pre><code>from sqlmodel import Field, SQLModel\nfrom sqlmodel_cache import CacheConfig\n\nclass Hero(SQLModel, table=True):\n    __cache_config__ = CacheConfig(ttl=600)  # 10-minute TTL for this model\n    id: int | None = Field(default=None, primary_key=True)\n    name: str = \"\"\n</code></pre>"},{"location":"quickstart/#3-use-sessionget-caching-is-transparent","title":"3. Use <code>session.get()</code> \u2014 caching is transparent","text":"<pre><code>from sqlmodel import Session, create_engine\n\nengine = create_engine(\"sqlite:///heroes.db\")\nSQLModel.metadata.create_all(engine)\n\n# First call \u2014 cache miss: queries DB, populates Redis\nwith Session(engine) as session:\n    hero = session.get(Hero, 1)\n\n# Second call \u2014 cache hit: no SQL issued, result from Redis\nwith Session(engine) as session:\n    hero = session.get(Hero, 1)\n</code></pre>"},{"location":"quickstart/#4-automatic-invalidation-on-commit","title":"4. Automatic invalidation on commit","text":"<p>When you modify and commit a cached model row, its cache key is automatically invalidated so the next <code>session.get()</code> fetches a fresh value:</p> <pre><code>with Session(engine) as session:\n    hero = session.get(Hero, 1)\n    hero.name = \"Updated Name\"\n    session.commit()  # cache key invalidated automatically\n\nwith Session(engine) as session:\n    hero = session.get(Hero, 1)  # cache miss \u2192 fresh DB read\n</code></pre>"},{"location":"quickstart/#async-quickstart-fastapi","title":"Async Quickstart (FastAPI)","text":"<p>Use the async transport with a FastAPI lifespan context manager:</p> <pre><code>import redis.asyncio\nfrom contextlib import asynccontextmanager\nfrom fastapi import FastAPI\nfrom sqlmodel_cache import SQLModelCache\nfrom sqlmodel_cache.transport import RedisAsyncTransport\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    r = redis.asyncio.Redis.from_url(\"redis://localhost:6379\")\n    SQLModelCache.configure(\n        transport=RedisAsyncTransport(r),\n        default_ttl=300,\n    )\n    yield\n    await r.aclose()\n\napp = FastAPI(lifespan=lifespan)\n</code></pre> <p>Async <code>session.get()</code> calls work identically:</p> <pre><code>from sqlmodel.ext.asyncio.session import AsyncSession\n\nasync with AsyncSession(async_engine) as session:\n    hero = await session.get(Hero, 1)  # cached transparently\n</code></pre>"},{"location":"quickstart/#per-call-control","title":"Per-call Control","text":"<p>Override caching for a single call:</p> <pre><code>with Session(engine) as session:\n    # Bypass cache for this call only\n    hero = session.get(Hero, 1, execution_options={\"cache\": False})\n\n    # Use a custom TTL for this call only\n    hero = session.get(Hero, 1, execution_options={\"cache_ttl\": 60})\n</code></pre> <p>Caching scope</p> <p>Only <code>session.get(Model, pk)</code> calls are intercepted and cached. Queries using <code>session.exec(select(Model))</code> pass through to the database as normal.</p>"},{"location":"quickstart/#next-steps","title":"Next Steps","text":"<ul> <li>API Reference \u2014 full class and method documentation</li> <li>Migration Guide \u2014 adding caching to an existing SQLModel app</li> <li>Advanced \u2014 async patterns, per-call bypass, test isolation</li> </ul>"}]}